/**
 * 기술적 지표 계산 (순수 함수).
 * 이동평균/RSI/변동성/MDD 등은 Phase 3 에서 구현한다.
 * LLM 은 계산하지 않고, 여기서 계산한 값을 입력으로 받는다.
 */

// Phase 3 placeholder. 예) export const movingAverage = (values, window) => { ... };
export {};
